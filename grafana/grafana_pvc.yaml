apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pvc
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteMany # Multiple nodes can read/write at a time
  resources:
    requests:
      storage: 5Gi 

